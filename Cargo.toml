[package]
edition = "2018"
authors = ["sopium"]
license = "GPL-3.0"
name = "titun"
repository = "https://github.com/sopium/titun"
version = "0.0.6-preview"

[workspace]
members = ["ip-lookup-trie"]
exclude = ["patched"]

[[bin]]
doc = false
name = "titun"
path = "src/main.rs"

[features]
bench = []

[dependencies]
arrayvec = "0.4.6"
base64 = "0.9"
byteorder = "1"
bytes = "0.4"
clap = "2.19.3"
env_logger = "0.6"
failure = "0.1"
fnv = "1.0.6"
futures-preview = { version = "0.3.0-alpha.16", features = ["compat", "nightly", "async-await"] }
hex = "0.3"
ip-lookup-trie = { path = "./ip-lookup-trie" }
log = "0.4.3"
mio = "0.6"
net2 = "0.2.33"
nix = "0.13"
noise-protocol = "0.0.5"
num_cpus = "1.9.0"
parking_lot = "0.7"
pin-utils = "0.1.0-alpha.4"
rand = "0.6"
ring = { path = "patched/ring" }
tai64 = {version = "0.2", default-features = false}
tokio = { version = "0.1.17", features = ["async-await-preview"] }
tokio-signal = "0.2"
tokio-threadpool = "0.1.11"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["fileapi", "winbase", "ioapiset", "synchapi", "namedpipeapi"] }
winreg = "0.6"

[target.'cfg(not(any(target_arch = "x86", target_arch = "x86_64")))'.dependencies]
packed_simd = { version = "0.3", features = ["into_bits"] }

[patch.crates-io]
tokio-async-await = { path = "patched/tokio-async-await-0.1.7" }
tokio = { path = "patched/tokio-0.1.19" }

[profile.release]
lto = true
